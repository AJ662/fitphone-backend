# Runtime deps for the deployed API. ray[tune] is required because RecBole's
# quick_start module imports it on load; kmeans-pytorch / thop / tensorboard
# are touched by RecBole's training graph but are tiny enough to include.
recbole==1.2.0
torch>=1.13,<2.3
ray[tune]>=2.0
kmeans-pytorch>=0.3
tensorboard>=2.10
thop>=0.1
pandas>=2.0
numpy>=1.24,<2.0
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0
pyyaml>=6.0
python-multipart
huggingface_hub>=0.24
